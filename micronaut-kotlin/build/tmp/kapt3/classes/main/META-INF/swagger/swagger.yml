openapi: 3.0.1
paths:
  /health:
    get:
      operationId: checkHealth
      parameters: []
      responses:
        default:
          description: checkHealth default response
          content:
            application/json:
              schema:
                type: string
